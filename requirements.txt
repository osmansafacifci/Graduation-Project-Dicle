# Minimal dependencies for the active SOPv2 pipeline.
#
# This replaces the former split requirements file from the older repo.
# It declares only what the active pipeline imports, with no legacy upper
# bounds that break installs on newer Python versions.
#
# Install:
#     pip install -r requirements.txt
#
# Compatible with Python 3.10–3.13.

# data fetching
gdown>=5.2.0

# core scientific stack (latest stable wheels — no version ceilings)
numpy>=1.26
pandas>=2.1
scipy>=1.11
h5py>=3.10

# modeling
scikit-learn>=1.4
mapie>=1.4
shap>=0.50
xgboost>=2.0
catboost>=1.2
